
export enum VaribaleSize {
    SIZE_AVOID_TOP = '--size-avoid-top',
    SIZE_AVOID_BOTTOM = '--size-avoid-bottom',
    SIZE_AVOID_LEFT = '--size-avoid-left',
    SIZE_AVOID_RIGHT = '--size-avoid-right',
    COLOR_TEXT = '--color-text',
    COLOR_BACKGROUND = '--color-background',
}